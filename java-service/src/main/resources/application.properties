logging.level.com.agrowcrop=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
server.port=8080
spring.application.name=agrowcrop-service

# MongoDB Configuration
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://kartikeyahirwar3_db_user:F3oWN2n0kNmU4Al2@agrowcrop.xejhehy.mongodb.net/?appName=agrowcrop}
spring.data.mongodb.database=agrowcrop

# Data.gov.in API Configuration
agmarknet.api.url=https://api.data.gov.in/resource/9ef84268-d588-465a-a308-a864a43d0070
agmarknet.api.key=${AGMARKNET_API_KEY:YOUR_API_KEY_HERE}

# SMS Provider
# Options: console (default), twilio
sms.provider=${SMS_PROVIDER:console}
twilio.account.sid=${TWILIO_ACCOUNT_SID:}
twilio.auth.token=${TWILIO_AUTH_TOKEN:}
twilio.phone.number=${TWILIO_PHONE_NUMBER:}

# OAuth2 / Clerk (Prod Profile)
spring.security.oauth2.resourceserver.jwt.issuer-uri=${CLERK_ISSUER_URI:https://dominant-chow-43.clerk.accounts.dev}
spring.security.oauth2.resourceserver.jwt.audiences=agrowcrop-api

# Enable Scheduling
spring.task.scheduling.pool.size=5

# CORS configuration (handled in SecurityConfig, but good to keep basics)
spring.web.cors.allowed-origins=${FRONTEND_URL:http://localhost:5173},http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Security Configuration
spring.security.filter.dispatcher-types=REQUEST,ERROR,ASYNC
